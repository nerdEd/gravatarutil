# -*- ruby -*-

require 'rubygems'
require 'hoe'
require './lib/gravatarutil.rb'

Hoe.new('gravatarutil', Gravatarutil::VERSION) do |p|
  # p.rubyforge_name = 'gravatarutilx' # if different than lowercase project name
  # p.developer('FIX', 'FIX@example.com')
end

# vim: syntax=Ruby
